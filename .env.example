# Copiar a .env (gitignored) y completar. NUNCA commitear .env ni la contraseña del NDA.

# Contraseña del NDA de SoccerNet (KAUST). Necesaria recién en Fase 1 (descarga de
# videos). Dejar vacía en Fase 0.
SOCCERNET_PASSWORD=

# Configuración de la API (pisa configs/api.yaml; prefijo API_).
API_LOG_LEVEL=info
API_PORT=8000
